plugins {
  id("java-platform")
}

indra {
  configurePublications {
    from components.javaPlatform
  }
}

dependencies {
  constraints {
    [
      "api"
    ].each {
      api(project(":event-$it"))
    }
  }
}
